// Single source of truth for the app version. Loaded by index.html (as a
// classic <script>) and by sw.js (via importScripts). Bump this and run
// `make deploy`.
'use strict';
const APP_VERSION = '0.4.3';
